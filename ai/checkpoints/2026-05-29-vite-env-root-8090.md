# Checkpoint: Vite lee .env raiz y puerto 8090

Fecha: 2026-05-29

Se ajusto la configuracion para backend en puerto 8090:
- Vite ahora lee variables desde el `.env` de la raiz con `envDir: '..'`.
- `VITE_API_BASE_URL` fallback pasa a `http://localhost:8090`.
- Se actualizaron comentarios de Google OAuth y documentacion.

Nota: despues de cambiar variables `VITE_*`, hay que reiniciar el dev server de Vite.
